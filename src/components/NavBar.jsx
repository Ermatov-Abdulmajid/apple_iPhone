import { appleImg } from "../utils";

const NavBar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} alt="logo-apple" width={14} height={18} />

        <div>
          
        </div>
      </nav>
    </header>
  );
};

export default NavBar;
